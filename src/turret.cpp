//#include "turret.h"

#include "demo.h"

int main(int argc, char *argv[]){
    
    // 
    
    while(1){
        
        // Take in set of class/cordinate/probability pairs
        // (calls Darknet code)
        
        
        // If matches target class && If meets threshold 
        // && If safety conditions satisfied:
            // Calculate required motor movement
            // Send a "MOVE" message to Arduino
            
            // Calculate distance between target class cordinates and 
            // center of video stream (assumes calibration between camera and line of fire)
            
            // If distance < threshold:
                // Send "FIRE" message to Arduino
        
        
        
    }
    
    
}
